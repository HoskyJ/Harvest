using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEditor;
using UnityEngine.UI;

public class Test : MonoBehaviour {

	public GameObject t;
	// Use this for initialization
	void Start () {
		//this.gameObject.GetComponent<Image>().sprite =  Sprite.Create(AssetPreview.GetAssetPreview(t), new Rect(0.0f, 0.0f, AssetPreview.GetAssetPreview(t).width, AssetPreview.GetAssetPreview(t).height), new Vector2(0.5f, 0.5f), 100.0f);
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
